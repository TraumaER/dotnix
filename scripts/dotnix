#!/usr/bin/env bash
set -euo pipefail
script_dir=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)
if command -v python3 >/dev/null 2>&1; then
  exec python3 "$script_dir/dotnix.py" "$@"
fi
exec nix --extra-experimental-features 'nix-command flakes' run "$script_dir/..#dotnix" -- "$@"
